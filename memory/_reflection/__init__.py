"""Internal implementation modules for :mod:`memory.reflection`.

The public and test-observable compatibility surface remains in
``memory.reflection``.  These modules contain only stable, side-effect-free
building blocks so the facade keeps ownership of I/O and monkeypatch seams.
"""
